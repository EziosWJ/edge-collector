# SQLite profile overlay used by task db:migrate:sqlite, api:sqlite and dev:sqlite.
# APP_ENV remains dev/test/prod; this file only switches the database backend.
# The relative path is resolved from the base-go-api directory.
database:
  driver: sqlite
  url: .data/base-go-api.db
  username: ""
  password: ""
