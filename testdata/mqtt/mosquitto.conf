# Deliberately use only standard Mosquitto features. The ADR-0017 harness must
# remain portable across a normal MQTT 5 broker and its MQTT 3.1.1 surface.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_type all
connection_messages true
log_timestamp true
