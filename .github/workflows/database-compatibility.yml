name: Database compatibility gate

on:
  push:
    paths:
      - "edge-collector-api/**"
      - "Taskfile.yml"
      - ".github/workflows/database-compatibility.yml"
  pull_request:
    paths:
      - "edge-collector-api/**"
      - "Taskfile.yml"
      - ".github/workflows/database-compatibility.yml"

permissions:
  contents: read

jobs:
  backend-check:
    name: Backend unit tests and vet
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: edge-collector-api
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: edge-collector-api/go.mod
          cache-dependency-path: edge-collector-api/go.sum
      - name: Run backend check
        run: |
          go test ./...
          go vet ./...

  sqlite-contract:
    name: SQLite migration, API and backup contract
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: edge-collector-api
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: edge-collector-api/go.mod
          cache-dependency-path: edge-collector-api/go.sum
      - name: Check migration streams
        run: go test ./migrations
      - name: Run SQLite integration contract
        run: go test -tags=integration ./integration -run SQLite -count=1

  postgres-contract:
    name: PostgreSQL integration contract
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: edge-collector-api
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: edge-collector-api/go.mod
          cache-dependency-path: edge-collector-api/go.sum
      - name: Run PostgreSQL integration contract
        run: >-
          go test -tags=integration ./integration
          -run 'Postgres|Auth|RBAC|SysConfig|Department|Dictionary|Login|File|Notification|UserPage'
          -count=1
